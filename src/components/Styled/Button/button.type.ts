import {
  TypographyProps,
  SpaceProps,
  BackgroundProps,
  ColorProps,
  LayoutProps,
  FlexProps,
  FlexboxProps,
  BorderProps,
} from "styled-system";

export type ButtonProps = TypographyProps &
  SpaceProps &
  BackgroundProps &
  ColorProps &
  LayoutProps &
  FlexProps &
  FlexboxProps &
  BorderProps;
